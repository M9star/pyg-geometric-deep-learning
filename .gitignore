# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.ipynb_checkpoints/

# PyG / torch downloads & cached datasets
data/
*/data/
*.pt

# Manim render output (videos are large).
media/

# YouTube upload package: rendered videos, thumbnails, per-video descriptions.
# These live locally / on YouTube, not in git — the repo links to the videos
# instead (see VIDEOS.md). Regenerate with:
#   uv run python animations/render_all.py --quality h
#   uv run python animations/make_thumbnails.py
youtube/
assets/*.mp4

# Jupyter notebook execution artifacts handled below

# OS / editor
.DS_Store
.idea/
.vscode/

# Keep the assets folder but ignore nothing inside by default —
# generated plots ARE committed so learners see expected output.
# (Remove the line below if you'd rather not commit images.)
# assets/*.png
