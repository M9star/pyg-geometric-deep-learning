# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.ipynb_checkpoints/

# PyG / torch downloads & cached datasets
data/
*/data/
*.pt

# Manim render output (videos are large).
media/

# Video production material lives locally / on YouTube, not in git — the repo
# links to the published videos instead (see the README's video table).
# This covers the Manim animation sources, the upload package (thumbnails +
# descriptions), the video guide, and all runtime-generated plots.
youtube/
animations/
assets/
VIDEOS.md

# Jupyter notebook execution artifacts handled below

# OS / editor
.DS_Store
.idea/
.vscode/

# Keep the assets folder but ignore nothing inside by default —
# generated plots ARE committed so learners see expected output.
# (Remove the line below if you'd rather not commit images.)
# assets/*.png
