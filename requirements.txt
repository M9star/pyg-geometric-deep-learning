# pip fallback — prefer `uv sync` (see pyproject.toml). Use Python 3.12.
#   python3.12 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt

# Core deep learning
torch>=2.2
torch-geometric>=2.5          # GPU / build issues? https://pytorch-geometric.readthedocs.io/en/latest/install/installation.html

# Science / plotting
numpy
scipy
scikit-learn                  # t-SNE embedding visualizations
matplotlib
networkx                      # graph drawing in the fundamentals module

# Notebooks
jupyter
ipykernel

# Narrated animations
manim>=0.18                   # also needs system ffmpeg + a LaTeX install
manim-voiceover[gtts]>=0.3.7  # gTTS narration backend (free, needs internet)
gtts
